config:
    # Type: one|ec2
    type: one
    # For amazon, use user with ssh keys, or put a keypair in .ssh and authorized_keys 
    ami_id:
    ami_type: m1.small
    ec2_access_key:
    ec2_secret_key: 
    ec2_url:
    ec2_port: 4567
    user_data:
